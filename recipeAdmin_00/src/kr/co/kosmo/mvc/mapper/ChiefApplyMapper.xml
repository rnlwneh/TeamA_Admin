<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chiefApply">



	<select id="newChief" resultType="cAply">
		select
		c.chef_req_no, m.mem_no, m.mem_id,c.chef_regdate,m.mem_email from chef_request
		c,
		member_info m where m.mem_no=c.mem_no
	</select>




	<update id="updateGrade">

		update member_info
		set
		mem_grade_no ='셰프'
		where mem_no=#{mem_no}

	</update>


	<delete id="delete">
		delete from chef_request
		where mem_no=#{mem_no}
	</delete>





</mapper>