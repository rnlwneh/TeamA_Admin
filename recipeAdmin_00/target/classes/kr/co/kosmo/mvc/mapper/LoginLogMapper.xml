<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="loginLog">

	<insert id="loginLog" parameterType="loginLog">
		insert into loginlog values(
			loginlog_seq.nextval
			, #{ad_no}
			, #{log_reip}
			, #{log_uagent}
			, #{log_status}
			, sysdate
		)
	</insert>
	
	<select id="adLogDetail" parameterType="int" resultType="loginLog">
		select 
			log_logtime
			, log_status
			, log_reip
		from loginlog 
		where ad_no=#{ad_no} 
		order by log_logtime desc
	</select>

</mapper>